# L1 Phase 2 范围说明（修订版）

**前置冻结：** L1 Phase 1 · `9f13877` · tag `l1-phase1-freeze-20260627`  
**实施分支：** `l1-phase2`  
**Phase 1 不改：** 决策优先级、失败包分拆、L2/L3、production_eligible

## 协议 v2 要点（已实现 2.1–2.3）

1. API 证据字段：`paragraph_id`（P0001 起）、`exact_text`、`occurrence_index`、`source_scope`（CURRENT_CHAPTER / PRIOR_CHAPTER）
2. `start_offset` / `end_offset` 由 Python 校验后计算写入报告
3. `analysis_summary` 必填且与证据分离；与 `exact_text` 相同仅 warning
4. 删除 API `overall` 与 `score`；`overall` 由代码按六维 verdict 计算
5. 跨章证据允许；各 scope 独立绑定验证语料
6. 正式项目前章顺序来自 manifest `chapter_sequence`；文件名排序仅开发回退（`context_quality=INFERRED`）
7. 三类重试：传输 / 格式 / 证据（证据重试允许改 verdict 与 summary）

## 冻结拆分

| 阶段 | 语料 | 结论 | 门槛 |
|---|---|---|---|
| **Phase 2A** | 3–5 章真实正文 + 人工标注 | `PILOT_VALIDATED` | 场景级硬验收 + 至少一次真实 API 全量 eval |
| **Phase 2B** | 20–30 章 | 是否生产验证通过 | 误杀/漏检/准确率等统计指标 |

Phase 2A **不得**使用 ≤10% 误杀/漏检、≤5% AUDIT_BLOCKED 作为有统计意义的生产门槛。

## 指标（分开记录）

- `first_pass_evidence_valid_rate`
- `eventual_evidence_valid_rate`
- `evidence_retry_rate` / `transport_retry_rate` / `format_retry_rate`
- `audit_blocked_rate`
- `overall_agreement` / `dimension_agreement`

recorded replay 仅用于协议回归；Phase 2A 冻结必须真实 DeepSeek API 全量评估。

## 验收集

目录：`tests/fixtures/l1_semantic_golden/`（当前 `AWAITING_CORPUS`，待用户提供 3–5 章匿名正文与 labels）

评估脚本：`脚本/评估_L1_语义金标准.py`

## 当前 L1 定性

```text
L1 工程决策架构：完成并冻结（Phase 1）
L1 证据协议 v2 + 上下文 + 重试：已实现（Phase 2 代码，未冻结）
L1 语义判断准确率：待 Phase 2A 真实语料验收
L1 生产资格：尚未获得
```
