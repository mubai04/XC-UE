修复内容：

1. 校验_L1_语义金标准.py
   - 五类场景覆盖同时统计 scenario_tag 与 secondary_scenario_tags。
   - 校验主/附加场景标签是否合法。

2. 评估_L1_语义金标准.py
   - 真实 API 评估同时统计主/附加场景标签。
   - 修复 acceptance_range 的读取路径：
     acceptance_range.overall
     acceptance_range.dimensions.<维度>
   - 正确读取 expected_dimensions.<维度>.expected_verdict。
   - 在章节结果中保留 scenario_tags，保证 PILOT 判定使用完整场景集合。

两个文件均通过 py_compile 语法检查。
