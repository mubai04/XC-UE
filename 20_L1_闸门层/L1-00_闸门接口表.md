# L1-00_闸门接口表.md

版本：v0.2
层级：L1 闸门接口层
状态：草稿 / 未认证 / 不可作为正式真源

---

## 1. 定位

L1-00 只定义 L1 闸门接口。

不解释各闸门内部细则。
不替代 L1-01 / L1-02 / L1-03。
不直接调用 L2。
不直接执行 Cursor。

---

## 2. 已有文件

| 文件    | 状态 | 作用     |
| ----- | -- | ------ |
| L1-01 | 已有 | 内部创作成立 |
| L1-02 | 已有 | 读者投入成立 |
| L1-03 | 已有 | 发布验收成立 |

---

## 3. 待建依赖

| 文件                     | 状态 | 作用           |
| ---------------------- | -- | ------------ |
| L1-E01_空项目恢复异常闸门.md    | 待建 | 判断项目是否进入异常恢复 |
| `../../30_L1.5_路由矩阵层/L1.5_Routing_Matrix.md` | 草稿 v0.3 | L1 失败后的路由矩阵  |

---

## 4. 闸门接口总表

| 闸门    | 必填输入                  | 可选输入           | 合法输出              | 失败枚举位置     | 失败去向        | 回流    |
| ----- | --------------------- | -------------- | ----------------- | ---------- | ----------- | ----- |
| L1-01 | 项目材料 / 章节材料 / 正文      | 世界观 / 角色 / 事件链 | 通过 / 失败 / 需派单     | L1-01 内部定义 | L1.5 路由矩阵 | L1-01 |
| L1-02 | 开头 / 章末 / 卖点 / 读者投入要素 | 读者反馈 / 平台目标    | 通过 / 失败 / 需派单     | L1-02 内部定义 | L1.5 路由矩阵 | L1-02 |
| L1-03 | 正文 / 字数 / 章末 / 发布锁状态  | 修改轮次 / 发布目标    | 发布 / 退回 / 降级 / 复盘 | L1-03 内部定义 | L1.5 或复盘 | L1-03 |

---

## 5. 统一输出格式

当前闸门：
输入材料：
判断结果：
失败类型：
失败位置：
是否进入 L1.5：
回流验收位置：
最终状态：

---

## 6. 流转规则

正常流：

```text
L1-01 → L1-02 → L1-03 → 发布执行
```

失败流：

```text
任一 L1 失败 → `30_L1.5_路由矩阵层/L1.5_Routing_Matrix.md` → 修复 → 回原 L1 复验
```

异常流：

```text
项目空转 / 连续失败 / 无输入 → 待建 L1-E01
```

---

## 7. 真源规则

L1-00 不维护各闸门失败类型细则。

失败类型细则由对应 L1 子文件维护。

L1-00 只维护接口与流转。

---

## 8. 禁止项

禁止 L1 直接写正文。
禁止 L1 直接调用 L2。
禁止 L1 替代 L1.5 派单。
禁止 L1-00 重复定义各 L1 细则。
禁止图片替代 Markdown 真源。
禁止只输出“通过/不通过”。

---

## 9. 验收条件

通过以下测试前，本文件不得定版：

* 10 个失败场景路由测试
* 至少 8 个场景能输出唯一处理路径
* 无重复真源
* 无待建依赖被误标为已建
