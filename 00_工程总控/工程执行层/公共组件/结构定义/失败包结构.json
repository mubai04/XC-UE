{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "xcue.schema/failure-packet/1.0",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "pipeline_run_id",
    "stage_run_id",
    "status",
    "failure_count",
    "blocking_count",
    "routeable_count",
    "items"
  ],
  "properties": {
    "schema_version": {
      "const": "xcue.failure-packet/1.0"
    },
    "pipeline_run_id": {
      "$ref": "#/$defs/run_id"
    },
    "stage_run_id": {
      "$ref": "#/$defs/run_id"
    },
    "status": {
      "$ref": "#/$defs/l1_status"
    },
    "failure_count": {
      "type": "integer",
      "minimum": 0,
      "maximum": 1000
    },
    "blocking_count": {
      "type": "integer",
      "minimum": 0,
      "maximum": 1000
    },
    "routeable_count": {
      "type": "integer",
      "minimum": 0,
      "maximum": 1000
    },
    "items": {
      "type": "array",
      "maxItems": 1000,
      "items": {
        "$ref": "#/$defs/failure_item"
      }
    },
    "extensions": {
      "$ref": "#/$defs/extensions"
    }
  },
  "$defs": {
    "run_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[0-9A-Za-z_\\-\\u4e00-\\u9fff]+$"
    },
    "l1_status": {
      "type": "string",
      "enum": [
        "SCREENING_PASS",
        "SCREENING_REVIEW",
        "SCREENING_REJECT",
        "AUDIT_BLOCKED"
      ]
    },
    "severity": {
      "type": "string",
      "enum": [
        "info",
        "warning",
        "error"
      ]
    },
    "text": {
      "type": "string",
      "maxLength": 2000
    },
    "short_text": {
      "type": "string",
      "maxLength": 256
    },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "段落",
        "摘句"
      ],
      "properties": {
        "段落": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 1,
          "maximum": 100000
        },
        "摘句": {
          "$ref": "#/$defs/text"
        },
        "段落ID": {
          "$ref": "#/$defs/short_text"
        },
        "source_scope": {
          "type": "string",
          "enum": [
            "CURRENT_CHAPTER",
            "PRIOR_CHAPTER"
          ]
        },
        "start_offset": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "maximum": 1000000
        },
        "end_offset": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "maximum": 1000000
        },
        "occurrence_index": {
          "type": "integer",
          "minimum": 0,
          "maximum": 1000
        }
      }
    },
    "failure_item": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "闸门",
        "名称",
        "状态",
        "说明",
        "证据",
        "严重级别",
        "失败类型",
        "候选模块",
        "回流验收位置",
        "修复方向",
        "decision_role",
        "blocking",
        "routeable",
        "route_reason",
        "source_component"
      ],
      "properties": {
        "闸门": {
          "$ref": "#/$defs/short_text"
        },
        "名称": {
          "$ref": "#/$defs/short_text"
        },
        "状态": {
          "type": "string",
          "enum": [
            "成立",
            "通过",
            "失败",
            "风险",
            "阻断",
            "检测到接口信号",
            "检测到代理信号"
          ]
        },
        "说明": {
          "$ref": "#/$defs/text"
        },
        "证据": {
          "type": "array",
          "maxItems": 200,
          "items": {
            "$ref": "#/$defs/evidence"
          }
        },
        "严重级别": {
          "$ref": "#/$defs/severity"
        },
        "失败类型": {
          "$ref": "#/$defs/short_text"
        },
        "候选模块": {
          "$ref": "#/$defs/short_text"
        },
        "回流验收位置": {
          "$ref": "#/$defs/short_text"
        },
        "修复方向": {
          "$ref": "#/$defs/text"
        },
        "heuristic": {
          "type": "boolean"
        },
        "signal_strength": {
          "$ref": "#/$defs/short_text"
        },
        "confidence": {
          "$ref": "#/$defs/short_text"
        },
        "decision_role": {
          "type": "string",
          "enum": [
            "HARD_GUARD",
            "CONTENT_DECISION",
            "DIAGNOSTIC",
            "AUDIT_BLOCKER"
          ]
        },
        "blocking": {
          "type": "boolean"
        },
        "routeable": {
          "type": "boolean"
        },
        "route_reason": {
          "type": "string",
          "maxLength": 256
        },
        "source_component": {
          "type": "string",
          "maxLength": 64
        },
        "reason_type": {
          "type": "string",
          "enum": [
            "",
            "INPUT_INVALID",
            "API_UNAVAILABLE",
            "EVIDENCE_INVALID",
            "SCHEMA_INVALID"
          ]
        }
      }
    },
    "extensions": {
      "type": "object",
      "additionalProperties": true,
      "maxProperties": 50
    }
  }
}
