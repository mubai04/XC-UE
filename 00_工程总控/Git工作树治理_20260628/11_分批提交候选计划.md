# 分批提交候选计划

不得使用 `git add .` / `git add -A` / `git commit -am`。

## 批次 A：唯一真源与治理

- 目标：S0治理文件、项目说明、索引、副本登记
- 允许路径前缀：00_工程总控/当前工程唯一真源, 00_工程总控/云盘副本, XC-UE_项目说明, 文档/项目治理层, .cursor/rules…
- 禁止路径：00_工程总控/工程执行层, tests/, 运行记录/
- 前置依赖：无
- 风险：低
- 可单独回滚：True
- 需要测试：False
- 人工确认：确认状态文档措辞与当前阶段一致

## 批次 B：L0、L1、L1.5定义与路由

- 目标：L0状态纠偏、L1权力模型、L1.5路由真源
- 允许路径前缀：10_L0, 20_L1, 30_L1.5, 00_工程总控/工程执行层/L1, 00_工程总控/工程执行层/L1.5, 脚本/校验_L1…
- 禁止路径：运行记录/, tests/fixtures/l2_real_api_pilot/results
- 前置依赖：['A']
- 风险：中
- 可单独回滚：True
- 需要测试：True
- 人工确认：路由规则条数与Matrix一致

## 批次 C：跨层契约与P0修复

- 目标：S2A v2 Schema、S2B-1迁移库、P0合法性、依赖锁定
- 允许路径前缀：00_工程总控/跨层接口契约, 00_工程总控/工程执行层/公共组件/结构定义/跨层契约, 00_工程总控/工程执行层/公共组件/跨层契约运行库, 脚本/校验_L0, 脚本/回放_v1, 依赖锁定, pyproject.toml, 文档/项目治理层/开发环境…
- 禁止路径：00_工程总控/工程执行层/L2工程/L2_0, 运行记录/
- 前置依赖：['B']
- 风险：中
- 可单独回滚：True
- 需要测试：True
- 人工确认：干净环境pytest已通过

## 批次 D：L2、L3及R4/R5实现

- 目标：领域实现、流水线、评测工具（按模块再拆）
- 允许路径前缀：00_工程总控/工程执行层/L2, 00_工程总控/工程执行层/L3, 40_L2, 50_L3, 脚本/评估, 脚本/生成, scripts/…
- 禁止路径：tests/fixtures/l2_real_api_pilot/results, 运行记录/
- 前置依赖：['C']
- 风险：高
- 可单独回滚：True
- 需要测试：True
- 人工确认：L2各模块分批提交; scripts仅包装

## 批次 E：测试语料与API评估记录

- 目标：fixtures、expected、R5试跑记录
- 允许路径前缀：tests/fixtures/, tests/test_, 审计纠偏_2026-06-26/L0_L3…
- 禁止路径：00_工程总控/工程执行层/
- 前置依赖：['D']
- 风险：中
- 可单独回滚：True
- 需要测试：True
- 人工确认：R5A-R5C原始记录是否入库

## 批次 F：清理提交

- 目标：运行产物删除、临时文件、ignore、过期状态
- 允许路径前缀：运行记录/, _t, _tmp, .gitignore…
- 禁止路径：tests/fixtures/, 审计纠偏_, 90_日志/
- 前置依赖：['A', 'B', 'C', 'D', 'E']
- 风险：高
- 可单独回滚：False
- 需要测试：True
- 人工确认：大量删除逐项确认; 不可重建证据不得删
