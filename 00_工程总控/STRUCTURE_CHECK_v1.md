# STRUCTURE_CHECK v1 — 目录结构自检

> 执行：2026-06-16  
> 用途：P3 自检，确认目录治理后无漂移、无混放、无误读风险。

---

## 1. 根目录检查

| 检查项 | 期望 | 结果 |
|---|---|---|
| 系统层目录 00–50 存在 | 是 | ✅ |
| `70_测试项目/` 存在 | 是 | ✅ |
| `90_日志/` 存在 | 是 | ✅ |
| `99_归档_不要索引/` 存在 | 是 | ✅ |
| 根目录无 `旧工程/` | 否 | ✅ |
| 根目录无 `XC-UE_Project_Harness_*` | 否 | ✅ |
| 根目录无 `50_L3_执行协议层_v0.1.2/` | 否 | ✅ |
| 根目录有边界声明文件 | 是 | ✅ `README_XC-UE_当前结构与真源边界.md` |
| `.cursorignore` 排除归档 | 是 | ✅ |

---

## 2. L2 图片检查

| 检查项 | 期望 | 结果 |
|---|---|---|
| `40_L2_正式能力层/` 根下无 `.png` | 是 | ✅ |
| 图片在 `_images/` | 是 | ✅ 7 张 |
| L2-04 无「正式正式」 | 是 | ✅ 已改为「正式版式」 |
| L2-06 配图 | 可选 | ⚠️ 暂无（记录缺口） |

`_images/` 当前文件：

```text
L2-00_正式能力层定义_v0.2.png
L2-01_叙事结构能力.png
L2-02_文风语言能力.png
L2-03_角色心理能力.png
L2-04_创意设定能力_正式版式架构图_v0.2.1_精确重生成版.png
L2-05_市场体验能力.png
L2-99_能力层接口总表.png
```

---

## 3. 测试项目检查

| 检查项 | 期望 | 结果 |
|---|---|---|
| 路径 | `70_测试项目/TP-001_CleanHarness_IR_Runtime/` | ✅ |
| IR/ 存在 | 是 | ✅ |
| `_legacy_root_inputs/` 隔离 | 是 | ✅ |
| 不与 L0–L3 平级 | 是 | ✅ |

---

## 4. 归档检查

| 检查项 | 期望 | 结果 |
|---|---|---|
| `99_归档_不要索引/旧工程/` 存在 | 是 | ✅ |
| 归档不在根目录 | 是 | ✅ |
| `.cursorignore` 含 `99_归档_不要索引/` | 是 | ✅ |

---

## 5. 隐藏配置检查

| 检查项 | 结果 |
|---|---|
| `.cursor/` 目录 | 不存在 |
| `.cursor/rules` | 不存在 |
| 索引脚本 | 未发现 |

当前约束依赖：`README_XC-UE_当前结构与真源边界.md` + `.cursorignore` + `SOURCE_POLICY`。

---

## 6. 自检结论

```text
目录结构：通过
真源边界：通过
Cursor 误读风险：已降低（归档隔离 + 边界声明 + ignore）
待补：L2-06 配图；TP-001 IR 填充（P4，未启动）
```

**可以进入 P4（填 IR），但需人工确认后启动。**
