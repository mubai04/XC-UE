# Project Harness v0.2.1 修正说明

> 状态：干净测试项目壳 / 候选真源 / 待真实项目压测

## 本次修正

1. 移动根目录旧输入文件到 `_legacy_root_inputs/`。
2. 明确正式输入只认 `IR/`。
3. 新增 `MANIFEST_文件权限与真源表.md`。
4. 新增 `IR/IR-99_输入完整性检查.md`。
5. 新增 `runtime/RT-00_运行时索引.md`。
6. 新增 `tests/L3_执行任务单.md`。
7. 更新 `README_执行顺序.md`。
8. 更新 `00_项目说明.md`。

## 当前判断

v0.2.1 解决了 v0.2 的主要问题：

- 双真源
- IR 输入完整性
- Runtime 回写
- L3 执行任务入口
- 本地权限清单

## 下一步

可以进入真实小说项目填充阶段：

1. 先填 IR
2. 再写 ch01
3. 跑 L1-01 / L1-02 / L1-03
4. 失败进 L1.5
5. 必要时调 L2
6. L3 输出候选产物
7. runtime 记录状态变化
8. 回 L1 / L1.5 复验
