# TP-001 测试项目说明 v0.2.1

> 项目性质：XC-UE Project Harness / XC-UE 项目试验台  
> 状态：测试项目壳 / 非 XC-UE 系统真源  
> 用途：承载一个真实小说项目，用于压测 XC-UE 全链路。  
> 当前版本：Clean Project Harness + IR + 最小 Runtime  
> 禁止：不得反向覆盖 L0 / L1 / L1.5 / L2 / L3 真源。

---

## 1. 定位

本目录不是 SRN-U。  
本目录不是 Xuke·Chuangjie。  
本目录不是新的理论系统。  
本目录不是新的 L0-L3 架构。  
本目录不是 L4。

本目录只是一个小说项目样机，用来让 XC-UE 跑完整链路。

---

## 2. v0.2.1 修正点

1. 解决根目录旧输入与 IR 双真源问题。
2. 根目录 01-05 已移动到 `_legacy_root_inputs/`。
3. 正式项目输入只认 `IR/`。
4. 新增 `MANIFEST_文件权限与真源表.md`。
5. 新增 `IR/IR-99_输入完整性检查.md`。
6. 新增 `runtime/RT-00_运行时索引.md`。
7. 新增 `tests/L3_执行任务单.md`。
8. 明确 runtime 只记录状态迁移，不自动推演，不升 L4。

---

## 3. 当前结构

```text
70_测试项目/TP-001_CleanHarness_IR_Runtime/
├── 00_项目说明.md
├── MANIFEST_文件权限与真源表.md
├── IR/
├── runtime/
├── chapters/
├── tests/
├── logs/
├── snapshots/
└── _legacy_root_inputs/
```

---

## 4. 当前禁止

- 不重写 L0
- 不重写 L1
- 不重建 L1.5
- 不重建 L2
- 不重写 L3
- 不画新图
- 不从废弃归档反向覆盖当前工程
- 不把本测试项目当 XC-UE 系统真源
- 不把 runtime 当正式 L4
- 不让候选正文覆盖正式正文
- 不把 `_legacy_root_inputs/` 当正式输入
